import ExploreContainer from '../components/ExploreContainer';
import Layout from '../components/Layout/Layout';

const Bookings: React.FC = () => {
  return (
    <Layout title='Reservas'>
      <p>Mi lista de reservas</p>
    </Layout>
  );
};

export default Bookings;
